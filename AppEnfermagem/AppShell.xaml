<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AppEnfermagem.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AppEnfermagem.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False"
    Title="AppEnfermagem">

    <ShellContent
        Title="Loading"
        ContentTemplate="{DataTemplate local:LoadingPage}"
        Route="LoadingPage" />

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:HomePage}"
        Route="HomePage" />

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

    <ShellContent
        Title="Adm"
        ContentTemplate="{DataTemplate local:AdmPage}"
        Route="AdmPage" />

    <ShellContent
        Title="Apresentação"
        ContentTemplate="{DataTemplate local:ApresentacaoPage}"
        Route="ApresentacaoPage" />

    <ShellContent
        Title="Inicio"
        ContentTemplate="{DataTemplate local:InicioPage}"
        Route="InicioPage" />

</Shell>

